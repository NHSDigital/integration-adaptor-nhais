type: object
properties:
  given:
    type: array
    items:
      type: string
  family:
    type: string
    description: Family name (often called Surname).
  prefix:
    type: array
    description: Title
    items:
      type: string
